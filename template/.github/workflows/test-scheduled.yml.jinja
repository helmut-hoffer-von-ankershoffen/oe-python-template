name: "Scheduled Testing"

on:
  schedule:
    - cron: '0 6 * * *'

jobs:
  test-scheduled:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/_scheduled-test.yml

