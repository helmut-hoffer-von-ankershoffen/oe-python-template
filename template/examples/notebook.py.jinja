# /// script
# requires-python = ">=3.13"
# dependencies = [
#     "marimo",
#     "{{ pypi_distribution_name }}==0.0.1",
# ]
# ///


import marimo
from {{ import_package_name }}.utils import __version__

__generated_with = "0.13.0"
app = marimo.App(app_title=f"{{ project_icon }} {{ project_name }} v{__version__}", width="full")


@app.cell
def _():
    import marimo as mo
    from {{ import_package_name }}.hello import Service

    service = Service()
    message = service.get_hello_world()

    with mo.redirect_stdout():
        print(message)
    return


if __name__ == "__main__":
    app.run()
